{
  "name": "setpr-landing",
  "version": "1.0.0",
  "private": true,
  "description": "getsetpr.com — static landing page and hosted legal documents. No build step; the repo is served as-is by GitHub Pages.",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/*.test.mjs'"
  },
  "engines": {
    "node": ">=21"
  }
}
